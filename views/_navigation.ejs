<nav class="row wide">
    <div class="left">
        <img src="../images/logo_mini.png">
    </div>
    <div class="right middle-align">
        <% if(!isauth) { %>
            <div class="button_top inline-block right-align">
                <a href="auth/facebook">Log in</a>
            </div>
        <% } else { %>
         <div class="button_top inline-block right-align">
                <p><%= user.name %></p>
                <a href="/logout">Log out</a>
            </div>
        <% } %>

        <img src="<%= !isauth ? '../images/user_prof.png' : 'http://graph.facebook.com/v2.1/'+user.uid+'/picture?type=normal' %>" class="round-image inline-block" />
    </div>
</nav>
